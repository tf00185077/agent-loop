{
  "name": "auto-agent",
  "version": "1.0.0",
  "description": "Goal-driven agent loop dashboard and runtime",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "dev:api": "tsx watch src/backend/server.ts",
    "dev:web": "vite --host 127.0.0.1 --config src/dashboard/vite.config.ts",
    "dev": "node scripts/dev.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
