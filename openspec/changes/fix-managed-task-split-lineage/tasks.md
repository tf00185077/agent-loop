## 1. Test-first lineage contract

- [x] 1.1 Add failing `task-registry` tests for an eligible multi-child split, below-threshold parent, active/pending parent, wrong change, cycle/self edge, empty or non-smaller contract, one invalid sibling rollback, exact re-announcement, and post-split descendant mutation. Acceptance: every scenario fails for the intended pre-fix reason before production code changes.
- [x] 1.2 Add a failing `managed-task-repository` transaction test proving parent status, complete child/criterion set, and split audit commit together, while an injected insert/audit failure leaves parent, children, events, and counters unchanged. Acceptance: pre-fix code reproduces the parent-not-split inconsistency and partial-state assertions are explicit.
- [x] 1.3 Add failing shared-lineage evaluator tests for non-`split` parent with child, `split` without child, cross-change edge, missing/cyclic edge, valid nested split, and accepted leaf closure. Acceptance: the same fixture is asserted through both change archive and Goal completion and demonstrates their current disagreement before the fix.
- [x] 1.4 Add a failing manager regression: two substantive rejections, direct registration of a narrower child without re-delegating the parent, child review/delivery acceptance, active-change archive, next-change activation, reassessment, and completion. Acceptance: the test captures the current no-solution loop and requires the repaired event order.
- [x] 1.5 Run the red focused commands `node --import tsx --test src/runtime/agent-session/task-registry.test.ts`, `node --import tsx --test src/persistence/managed-task-repository.test.ts`, and `node --import tsx --test src/runtime/agent-session/managed-completion-evaluator.test.ts src/runtime/agent-session/change-registry.test.ts src/runtime/agent-session/agent-session-manager.test.ts`; record expected failing test names without weakening unrelated assertions.

## 2. Shared atomic lineage implementation

- [x] 2.1 Implement the pure staged narrowing validator and recursive durable lineage evaluator with stable `invalid_split_lineage` reason codes and bounded logical task IDs. Acceptance: all pure valid/invalid/frozen-child tests pass without archive or completion having separate graph rules.
- [x] 2.2 Implement the SQLite transaction that validates the entire child set, transitions the parent, inserts children/criteria, and appends `managed_task.lineage_split` atomically; exact re-announcement is a no-op. Acceptance: repository rollback/fault tests and foreign-key checks pass.
- [x] 2.3 Reorder the manager task-list path to persist first, rehydrate the task cache from committed SQLite, and link the change cache only after success; make the in-memory-only path stage and swap under the same validator. Acceptance: injected persistence/cache-refresh failures never produce an acknowledged partial lineage, and restart reconstructs committed state.
- [x] 2.4 Replace archive and completion lineage traversal with the shared durable projection while retaining their independent merge, candidate, reassessment, and delivery gates. Acceptance: both evaluators return the same lineage result and the earlier `repair-durable-completion-ledger` candidate-obligation tests remain green.
- [x] 2.5 Run focused lineage/repository/evaluator/manager tests and commit task group 2 per `CLAUDE.md`. Acceptance: the commit contains only the tested lineage contract and no archive reconciliation, migration, recovery, or continuation-budget redesign.

## 3. Migration and restart rehydration

- [x] 3.1 Add file-backed disposable migration fixtures for fresh, already-valid, provably incident-shaped, ambiguous chronology, non-narrower child, cross-change edge, pending parent pipeline, cycle, and terminal-blocked databases. Acceptance: fixtures snapshot tasks, Goals, runs, sessions, events, migration details, and `PRAGMA foreign_key_check` before implementation.
- [x] 3.2 Implement `managed-task-split-lineage-repair-v1` on the existing named migration ledger, including fresh-baseline handling, bounded repaired/ambiguous diagnostics, and transactional marker semantics. Acceptance: only provable parents change to `split`; raw audit and all terminal lifecycle rows remain byte-for-byte equivalent.
- [x] 3.3 Add migration crash/re-entry fault injection before row update, before marker insertion, and after successful commit. Acceptance: failed migration and marker roll back together, first reopen converges once, and second reopen is a no-op.
- [x] 3.4 Add restart fault injection after the durable split transaction but before cache refresh and during interrupted-Goal rehydration. Acceptance: restart projects the parent/children once from SQLite, preserves counts/contracts/change ownership, and starts no bootstrap re-decomposition.
- [x] 3.5 Run `node --import tsx --test src/persistence/database.test.ts src/persistence/managed-task-repository.test.ts src/runtime/agent-session/supervisor-state-rehydration.test.ts src/runtime/agent-session/managed-runtime-restart.test.ts` and commit task group 3. Acceptance: migration/restart suites pass twice against fresh disposable paths and no repository database is opened for write.

## 4. Backend-owned archive and durable blockers

- [x] 4.1 Add failing manager/workspace tests requiring `change.archive_blocked` for undelivered leaf, invalid lineage, unmerged changes, and ambiguous archive topology, with stable blocker types, task IDs, and sanitized payloads. Acceptance: the prior silent undelivered-parent return is reproduced before implementation.
- [x] 4.2 Add failing candidate-path tests that reject additions/modifications under `openspec/changes/archive/`, main-spec sync mutations, and active-change deletions while allowing normal active spec edits and production/test edits. Acceptance: a Worker-shaped `openspec archive` diff cannot reach delivery even if model output requests or approves it.
- [x] 4.3 Add the durable archive-operation schema/repository and write-ahead intent containing source, fixed dated target, validated manifest digest, pre-archive HEAD, status, and bounded diagnostics. Acceptance: intent exists before the first filesystem/Git call and final operation plus `change.archived` event commit exactly once.
- [x] 4.4 Implement fail-closed reconciliation for restart before move, after move, after Git commit, before final event, and after final event before cache activation. Acceptance: recognized intent states converge idempotently; both/neither/multiple paths, digest/HEAD mismatch, and target-without-intent remain blocked with no adoption or second archive.
- [x] 4.5 Wire every active archive attempt to one durable outcome, update/rehydrate change cache only after durable finalization, and verify next-change activation occurs once. Acceptance: no archive gate path returns silently and replay after restart matches the durable event/operation ledger.
- [x] 4.6 Run `node --import tsx --test src/runtime/agent-session/openspec-workspace-service.test.ts src/runtime/agent-session/change-registry.test.ts src/runtime/agent-session/agent-session-manager.test.ts src/runtime/agent-session/supervisor-state-rehydration.test.ts` and commit task group 4. Acceptance: focused archive, path-ownership, and fault-injection cases pass with clean disposable Git workspaces.

## 5. Explicit operator recovery for existing blocked Goals

- [x] 5.1 Add failing dry-run/apply tests for an eligible continuation-exhausted Goal, invalid/ambiguous lineage, active pipeline state, wrong terminal reason, missing backup, stale plan digest, multiple archives, invalid artifacts, digest mismatch, and incoherent Git provenance. Acceptance: dry-run is byte-for-byte read-only and every failed apply precondition has zero partial writes or file/Git changes.
- [x] 5.2 Implement a local offline recovery service/command that requires a stopped backend, verified backup, Goal ID, explicit apply flag, and exact dry-run digest. Acceptance: default invocation only reports the bounded plan; no provider, runtime, archive move, or direct ad-hoc SQL path is exposed.
- [x] 5.3 Implement explicit adoption of one proven legacy provider-created archive by recording operator authorization, a reconciled archive operation, and one `change.archived` event. Acceptance: apply never rewrites the archive, never adopts target-without-proof automatically, and a second identical invocation is a no-op.
- [x] 5.4 Transition an eligible recovered Goal only from `blocked` to `interrupted`, clear only its terminal timestamp, and preserve historical tasks/runs/sessions/events before normal restart resume. Acceptance: the recovery command starts no provider; restart creates a fresh continuation from durable projection rather than bootstrap.
- [x] 5.5 Run the recovery command twice in dry-run mode and once in apply mode on separate disposable copies of the sanitized incident-shaped database/workspace fixture; hash the originals before/after. Acceptance: dry-run plans are identical, the apply copy reaches coherent interrupted/archive state, ambiguous copies remain blocked, and original hashes are unchanged.
- [x] 5.6 Run focused recovery, goal repository, resume, migration, and archive tests and commit task group 5. Acceptance: all recovery paths are explicit, fail closed, and independent of continuation-budget implementation.

## 6. Continuation-policy compatibility

- [x] 6.1 Add regression assertions that the configured continuation maximum, completion-less/control-rejected classification and increments, reset behavior, and exhaustion messages remain unchanged outside repaired lineage flows. Acceptance: no production diff in this change modifies continuation policy or uses budget reset/bypass to make the regression pass.
- [x] 6.2 Verify the repaired manager scenario archives and activates the next change because durable lineage is correct, not because continuation accounting changed. Acceptance: event order contains `managed_task.lineage_split` → accepted child/delivery → `change.archived` → next `change.activated`, with no `supervisor.continuations_exhausted`.
- [x] 6.3 Commit task group 6 per `CLAUDE.md`. Acceptance: the commit is tests/documentation unless an existing diagnostic needs mechanical compatibility wiring; any product policy change is removed and proposed separately.

## 7. Full and real-Goal verification

- [x] 7.1 Run all focused suites for task registry, managed-task repository, database migration, shared completion/archive evaluation, manager, workspace archive, restart rehydration, interrupted resume, and operator recovery. Acceptance: every focused command passes from a clean process and disposable database/workspace.
- [x] 7.2 Run `npm run typecheck` and `npm test`. Acceptance: both exit successfully; any pre-existing platform-specific exclusion is identified with baseline evidence and is not broadened by this change.
- [x] 7.3 Run a deterministic product-level staged-pipeline regression that forces two substantive parent rejections and direct child registration, injects restarts after split commit and archive move, then completes subsequent changes and reassessment. Acceptance: one frozen split, one archive per change, one activation per next change, no inactive-change/reassessment/review-without-result rejection loop, and no continuation exhaustion.
- [x] 7.4 Run a disposable real large Goal using real Codex Supervisor, Worker, and `review_merge` roles plus independent SQLite and Goal workspace, based on the recorded staged task-pipeline shape. Acceptance: durable events prove parent/child lineage consistency across any narrowing, all required leaves are accepted/delivered, every change archives in order, final reassessment is satisfied, Goal completes, and zero `supervisor.continuations_exhausted` events occur; record provider/session/change/task/event counts and exact terminal message.
- [x] 7.5 Exercise real restart fault windows with test-only injection at split commit/cache refresh and archive move/final event on disposable state, then restart normally. Acceptance: no duplicate child, archive, activation, delivery, or provider session is created and the run converges or fails closed with a durable diagnostic.
- [x] 7.6 Run `openspec validate fix-managed-task-split-lineage --strict`, `git diff --check`, and `git status --short --untracked-files=all`; record commands, outputs, migration fixture evidence, restart evidence, incident-copy hashes, and live E2E timeline in `verification.md`, then commit task group 7. Acceptance: validation is strict, diff check is clean, and only intended implementation/tests/docs are present.

## 8. Independent-review hardening

- [x] 8.1 Add RED/GREEN durable regressions for cross-Goal parent joins, frozen split evidence through both archive and completion, normal child-before-task-list migration chronology, runtime split migration evidence, and `ambiguousParents`/legacy `ambiguousTasks` compatibility. Acceptance: all invalid graphs fail closed with stable diagnostics and provable historical graphs retain frozen child IDs.
- [x] 8.2 Add RED/GREEN real-Git archive regressions for dirty/staged state, exact scoped source-to-target commits, unique pending-restart commit identity, zero/conflicting/mismatched proofs, real Git staging failure, and manager exception persistence. Acceptance: unrelated state is never committed, current HEAD is never adopted without proof, and every attempt is durably archived, blocked, or failed.
- [x] 8.3 Add RED/GREEN restart/recovery regressions for post-commit cache refresh failure, real pending-delivery reset failure, stale stopped evidence, and authorization replay with tampered Goal/archive/event/cross-links. Acceptance: restart/provider resume occurs only from verified durable postconditions; every ambiguous or partial state remains durably non-resumable.
- [x] 8.4 Rerun all affected focused suites, typecheck, full tests where the environment permits, strict OpenSpec validation, diff check, and final untracked-file audit; remove the temporary `node_modules` symlink and record exact results without claiming real-provider E2E. Acceptance: focused/typecheck/OpenSpec/diff gates pass, platform-only full-suite blockers are explicit, and the worktree contains no dependency installation or unexpected file.

## 9. Second independent-review fail-closed hardening

- [x] 9.1 Add a distinct-time migration RED proving that one child created after otherwise matching split evidence keeps the complete frozen set ambiguous, then require every child creation to predate or equal the evidence. Acceptance: the pre-fix fixture incorrectly repairs the parent; GREEN records `ambiguous_chronology` and leaves it unchanged.
- [x] 9.2 Add restart RED cases for a pending delivery with no checkpoint and a delivery service with no reconciler, then durably block both before any resumable transition. Acceptance: each case records one stable `recovery.reconciliation_blocked` blocker, no `recovery.reconciled`, and zero provider starts.
- [x] 9.3 Add a durable-manager RED proving a database-backed Goal falls through to legacy archive when `prepareArchive` is absent, then prohibit that downgrade while preserving the database-less compatibility path. Acceptance: legacy archive calls and `change.archived` remain zero and `archive_capability_unavailable` is durable.
- [x] 9.4 Add real-Git idempotent replay RED cases for deleted and modified archive targets plus unrelated dirty state, then revalidate topology, current manifest, cleanliness, and the recorded coherent archive commit. Acceptance: every replay is read-only, non-idempotent, and fail closed with a stable blocker.
- [x] 9.5 Rerun all affected focused suites, typecheck, full tests where the environment permits, strict OpenSpec validation, diff check, and untracked-file audit; remove the temporary `node_modules` symlink and record exact results without claiming real-provider E2E. Acceptance: all non-platform gates pass, any listener-only full-suite blocker is explicit, and no dependency installation, commit, push, archive, or external Goal/provider/runtime mutation occurred.

## 10. Third independent-review durable proof hardening

- [x] 10.1 Add RED/GREEN durable fixtures for an accepted, passing task named by the frozen-contract migration ambiguity marker and require completion, active-change archive, and operator recovery to fail closed with stable `ambiguous_frozen_contract` diagnostics while preserving split-lineage marker compatibility. Acceptance: neither marker schema is ignored and no ambiguous task can satisfy a terminal gate.
- [x] 10.2 Add a real-Git injected archive race between preparation and commit, then revalidate the moved target and unique commit-tree manifest through one canonical source-to-target mapping before durable finalization. Acceptance: tampered content never returns archive success, finalizes an archive operation, or activates the next change; operator recovery replay uses the same canonical manifest proof.
- [x] 10.3 Add a restart RED for a database-backed interrupted Goal with an active change, no archive operation, and no durable archive preparation capability, then persist `archive_capability_unavailable` and prevent provider start. Acceptance: only the intentionally database-less compatibility path retains legacy archive behavior.
- [x] 10.4 Add a real-Git pending-delivery RED with an ignored generated artifact and enforce the documented non-disposable supervisor policy: protected ignored inputs are retained, while any uncontrolled ignored artifact makes checkpoint proof fail closed and prevents a resumable state. Acceptance: recovery records one sanitized reset blocker and never starts a provider; disposable child worktrees remain owned by whole-worktree cleanup.
- [x] 10.5 Rerun affected focused suites, typecheck, full tests where the environment permits, strict OpenSpec validation, diff check, and final untracked-file audit; remove the temporary `node_modules` symlink and record exact results without claiming real-provider E2E. Acceptance: all available non-platform gates pass, remaining environmental blockers are explicit, and no commit, push, archive, external Goal/provider/runtime mutation, or dependency installation occurred.

## 11. Complete frozen-contract enforcement identity

- [x] 11.1 Add a deterministic 51-task migration RED/GREEN proving the bounded `ambiguousTasks` array remains 50 entries while a separate complete enforcement identity contains ambiguity 51 and preserves accepted/PASS historical rows. Acceptance: the marker count and complete identity agree, the 51st task is omitted only from human diagnostics, and foreign keys remain valid.
- [x] 11.2 Add RED/GREEN completion, active-change archive, and offline recovery regressions for ambiguity 51, plus legal-Goal and already-written truncated-v1 compatibility cases. Acceptance: every named affected Goal/task fails closed, new complete markers do not block an unnamed legal Goal, and a legacy marker whose omitted owners are unknowable fails closed globally with stable sanitized diagnostics.
- [x] 11.3 Rerun affected focused suites, restart regression, typecheck, full tests where the environment permits, strict OpenSpec validation, diff check, and final untracked-file audit; remove the temporary `node_modules` symlink and record exact results without claiming real-provider E2E. Acceptance: every non-listener gate passes, listener-only blockers remain explicit, and no commit, push, archive, external Goal/provider/runtime mutation, or dependency installation occurred.

## 12. Invalid frozen-contract marker details

- [x] 12.1 Add malformed-JSON and valid-JSON-non-object RED/GREEN fixtures for completion, active-change archive, and offline operator recovery. Acceptance: both invalid persisted payloads produce global `ambiguous_frozen_contract`; archive records `change.archive_blocked` before workspace execution and recovery remains read-only/ineligible.
- [x] 12.2 Preserve marker-shape compatibility with explicit controls for marker absence, valid fresh baseline, valid zero ambiguity, task-scoped old markers, complete enforcement identities, and legacy truncated global markers. Acceptance: only present-but-invalid details change behavior and the complete/legacy enforcement rules remain unchanged.
- [x] 12.3 Run all affected focused suites, typecheck, strict OpenSpec validation, diff check, and final untracked-file audit; remove the temporary `node_modules` symlink and record exact RED/GREEN evidence without claiming full or real-provider E2E. Acceptance: all requested gates pass and no commit, push, archive, external Goal/provider/runtime mutation, non-test SQLite write, or dependency installation occurs.
