# GitHub token (擇一設定即可)
# 優先順序: GITHUB_TOKEN > GH_TOKEN > COPILOT_GITHUB_TOKEN

# 方式一：gh auth token 取得的 token（推薦）
# GITHUB_TOKEN=***
# 方式二：GitHub PAT (classic)，需 read:user scope
# GITHUB_TOKEN=ghp_...

# 方式三：不設定，第一次執行會跳引導式登入
